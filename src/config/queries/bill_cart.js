const table = 'bill_cart';

module.exports = {
    // Insert
    createBillCart: `INSERT INTO ${ table } (cart_ide, bill_ide) 
    VALUES ($1, $2)`,    
   
    
    // Select
    
    
    // Update
    

    // Delete
};